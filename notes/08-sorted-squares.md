# LeetCode 977 有序数组的平方

## 我的思路（20分钟）
企图用双指针，但绕不清，最后选择排序
```python
def sortedSquares(nums):
    return sorted(x**2 for x in nums)
```
## 标准解法
左右指针：两端向中间收敛，从后往前填充
关键：平方后最大值只可能在两端，比较后填入

## 教训
- 看到"有序数组" + "平方/绝对值" → 想到左右指针
- 从后往前填充是反直觉的，需要刻意练习
- 左右指针 vs 快慢指针：场景不同，要区分