# 每日学习总结 2026-08-01

## 完成内容

- [x] LeetCode 1143 最长公共子序列（50分钟，中等）
  - 前20分钟想一维DP，状态定义卡壳，状态转移方程推导失败
  - 看答案后理解二维DP：dp[i][j] 表示 text1前i个与text2前j个的最长公共子序列长度
  - 相等时 dp[i][j] = dp[i-1][j-1] + 1，不相等时 max(dp[i-1][j], dp[i][j-1])

- [x] LeetCode 718 最长重复子数组（15分钟，中等）
  - 7分钟写出框架（基于1143思路调整，要求连续子数组）
  - 调试时二维数组行列初始化下标越界，m和n搞反，修正后通过

- [x] Go项目重构（约2小时）
  - 单文件 main.go 拆分为 MVC 风格目录结构
  - 7步逐步完成：models → cache/queue → middleware → handlers → routes → main.go → tests
  - 各包职责清晰，内部import统一为 go-gin-blog/xxx

- [x] Go项目代码检查修复
  - JWT密钥环境变量化（优先读取 JWT_SECRET）
  - 文章更新/删除添加作者权限校验（对标FastAPI）
  - Dockerfile构建命令适配（go build . 编译整个模块）
  - 错误处理完善（密码加密/Token生成不再静默忽略）
  - Redis连接增加Ping验证
  - 草稿路由Swagger路径统一为 /articles/user/drafts
  - 修复重复日志中间件（gin.Default() → gin.New() + Recovery）
  - UnlikeArticle用户查询增加错误检查
  - GetCategoryArticles增加Preload("Category")防N+1
  - 测试数据库路径改为 os.TempDir()

## 统计数据
- GitHub commit：约3个
- 学习时长：约5小时（15:19-21:24）
- LeetCode累计：83题（1143+718=2题新）
- 项目重构：Go项目从单文件拆分为MVC目录结构，代码检查11项修复

## 关键突破
- 1143最长公共子序列：二维DP核心，dp[i][j] 定义 + 相等↘+1 / 不相等max(←,↑)
- 718最长重复子数组：连续子数组要求，相等时延续，不相等时置0
- Go项目MVC重构：按职责分层，models/cache/queue无循环依赖，handlers引用models/cache/queue
- 权限校验：文章操作校验作者身份，安全实践对齐FastAPI
- 错误处理：不再静默忽略，Redis启动即验证连接

## 踩坑记录
- 1143题：一维DP状态定义卡壳20分钟，二维DP才是正解
- 718题：二维数组行列初始化时m和n搞反，下标越界
- gin.Default() 内置Logger与自定义Logger叠加，导致每条请求日志重复输出

## 动态规划二维DP总结

| 题号 | 名称 | dp[i][j]含义 | 状态转移 | 类型 |
|------|------|-------------|---------|------|
| 1143 | 最长公共子序列 | text1前i与text2前j的LCS长度 | 相等↘+1，不相等max(←,↑) | 子序列DP |
| 718 | 最长重复子数组 | 以i-1,j-1结尾的最长重复长度 | 相等↘+1，不相等置0 | 子数组DP |

## Go项目重构与代码检查修复

| 优化点 | 文件 | 操作 |
|--------|------|------|
| 目录重构 | 全项目 | 单文件→models/middleware/handlers/routes/cache/queue/tests |
| JWT密钥 | middleware/jwt.go | 环境变量化 |
| 权限校验 | handlers/article.go | 更新/删除校验作者身份 |
| Swagger修正 | handlers/*.go | 类型引用加 models. 前缀 |
| Dockerfile | Dockerfile | go build 目标改为 . |
| 错误处理 | handlers/user.go | 密码加密/Token生成错误处理 |
| Redis验证 | cache/cache.go | 增加Ping检查 |
| 路由统一 | handlers/article.go | ListDrafts路径修正 |
| 日志修复 | routes/router.go | gin.Default → gin.New + Recovery |
| 错误检查 | handlers/like.go | UnlikeArticle用户查询加校验 |
| N+1优化 | handlers/category.go | Preload("Category") |
| 测试路径 | tests/main_test.go | 改为 os.TempDir() |

## 明日计划（8月2日）
- [ ] LLM技术栈入门：LangChain安装+基础概念+API调用测试
- [ ] FastAPI+LLM集成设计：文章智能摘要、AI评论助手
- [ ] LeetCode新题（动态规划继续）
- [ ] 学习笔记整理