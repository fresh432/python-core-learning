# 每日学习总结 2026-09-06

## 完成内容

- [x] LeetCode 56 合并区间（60分钟，中等）
  - 前20分钟尝试线性解法未成功，改用库函数排序
  - 端点比较卡壳，尝试倒序遍历有bug，改回正序遍历
  - 合并时同步扩展右端点，保存区间并开启新区间，10分钟调试通过

- [x] LeetCode 238 除自身以外数组的乘积（40分钟，中等）
  - 前10分钟理解前缀积+后缀积思路
  - 正向遍历存前缀积到输出数组，倒序遍历计算后缀积并乘以前缀积
  - 30分钟实现，中间在是否用额外数组保存前缀后缀上花了时间

- [x] 代码修复①：rag.py Chroma字段名拼写错误
  - _all_chunks.get("document") → "documents"，BM25索引恢复正常
  - Hybrid Search现在能正常融合向量检索和关键词检索结果

- [x] 代码修复②：docker-compose.prod.yml Docker挂载冲突
  - 移除./logs/mysql到/var/lib/mysql的冲突挂载
  - mysql_data卷单独挂载到/var/lib/mysql存数据，./logs/mysql挂载到/var/log/mysql存日志

- [x] 代码修复③：agent.py eval安全隐患
  - eval(expression) → ast.parse + _safe_eval安全解析
  - 只允许数字常量和+-*/运算节点，彻底杜绝Unicode绕过等代码注入风险

- [x] 代码修复④：categories.py 分类删除权限校验
  - models/user.py新增role字段，默认"user"
  - delete_category增加current_user.role == "admin"校验，非管理员返回403

## 统计数据
- GitHub commit：约4个
- 学习时长：约5小时（16:29-23:37）
- LeetCode累计：154题

## 关键突破
- 56题：排序后贪心合并，正序遍历维护当前区间右端点，重叠则扩展，不重叠则保存
- 238题：两次遍历原地计算，输出数组先存前缀积，再倒序乘以后缀积，O(n)时间O(1)额外空间
- rag.py字段修复：Chroma返回字段名是复数"documents"，原拼写错误导致BM25永远为空
- docker-compose挂载分离：数据卷和日志目录独立挂载，避免覆盖导致数据丢失
- AST安全解析：ast.parse生成语法树，只允许Num/Constant和BinOp节点，比字符白名单更可靠
- 权限校验：新增role字段区分user/admin，修复任何登录用户都能删分类的逻辑漏洞

## 踩坑记录
- 56题：倒序遍历合并区间逻辑复杂易出bug，正序遍历+维护当前合并区间更直观
- 238题：前缀积和后缀积的索引对应关系易混淆，需明确output[i] = left[i] * right[i]
- categories.py权限校验：涉及模型变更（新增role字段），已注册用户需手动更新role为admin

## 明日计划（9月7日）

- [ ] LeetCode 2题
- [ ] 架构问题①：认证逻辑重复 → 提取get_current_user到dependencies.py统一维护
- [ ] 架构问题②：Agent流式接口内存风险 → _stream()里all_messages引用赋值修复
- [ ] 架构问题③：AI模块单元测试覆盖
- [ ] 架构问题④：RAG文档去重 + source字段追踪