# LeetCode 704 二分查找

## 我的思路（10分钟）
直觉：每次裁剪列表一半，用 count 记录偏移量
问题：切片创建新列表，时间 O(n)，代码复杂

## 标准解法
双指针：i, j 逻辑收缩，不修改原列表
关键：i <= j，mid = i + (j-i)//2，j = mid-1 / i = mid+1

## 教训
- 双指针想到过，但没用，因为直觉选择了"裁剪"
- 下次：先想"能不能用索引移动代替物理操作"
- 二分查找模板必须背熟