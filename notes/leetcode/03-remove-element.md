# LeetCode 27 移除元素

## 我的思路（5分钟）
直觉：`while True` + `nums.remove(val)`
问题：`remove()` 是 O(n)，整体 O(n²)

## 标准解法
双指针（快慢指针）：快指针扫描，慢指针写入
关键：遇到 `val` 跳过，否则写入慢指针位置

## 教训
- 看到"原地修改数组" → 想到双指针
- `remove()` 有隐藏成本，避免在循环中用
- 快慢指针：快指针永远前进，慢指针只在保留时前进